spring:
  application:
    name: nexa
    version: SNAPSHOT-1.0.0

spring.datasource:
    url: jdbc:postgresql://localhost:5432/nexa_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver